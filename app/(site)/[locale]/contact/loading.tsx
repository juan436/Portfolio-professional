import { ContactSkeleton } from "@/components/skeletons/page-skeletons"

/** Skeleton de `/contact` (header + rejilla de 3 columnas del chat). */
export default function Loading() {
  return <ContactSkeleton />
}
