import { LaboratorySkeleton } from "@/components/skeletons/page-skeletons"

/** Skeleton de `/laboratory` mientras carga (mismo layout: 2 expedientes + control). */
export default function Loading() {
  return <LaboratorySkeleton />
}
