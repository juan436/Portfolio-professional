import { BlogSkeleton } from "@/components/skeletons/page-skeletons"

/** Skeleton de `/blog` (destacado + lista editorial + temas). */
export default function Loading() {
  return <BlogSkeleton />
}
